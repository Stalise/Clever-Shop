import { FC } from 'react';

const MainPage: FC = () => {
    return (
        <div
            style={{
                display: 'flex',
                justifyContent: 'space-between',
                alignItems: 'end',
            }}
        ></div>
    );
};

export default MainPage;
